# Development Setup

Use Node.js 20+, enable pnpm with Corepack, copy `.env.example` to `.env`, run `pnpm install`, and start PostgreSQL with `docker compose up -d postgres`. Run `pnpm db:generate` to generate the Prisma client.
