# Development Workflow

Run focused app commands while developing and use the root Turborepo commands for repository-wide checks: `pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm typecheck`, and `pnpm format:check`. Keep business logic out of shared infrastructure and UI placeholders until the domain design phase begins.
